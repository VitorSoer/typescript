// Task: Convert a number to a string and print the inferred type.

const numberToConvert = 50;
const convertedVariable = String(numberToConvert);

console.log(
  `First variable: ${numberToConvert} - Type: ${typeof numberToConvert}`,
);

console.log(
  `Second variable: ${convertedVariable} - Type: ${typeof convertedVariable}`,
);
